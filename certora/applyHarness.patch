diff -ruN .gitignore .gitignore
--- .gitignore	1969-12-31 16:00:00.000000000 -0800
+++ .gitignore	2022-07-11 19:47:18.000000000 -0700
@@ -0,0 +1,2 @@
+*
+!.gitignore
--- compound/MorphoGovernance.sol	2022-06-21 09:26:03.000000000 -0700
+++ compound/MorphoGovernance.sol	2022-06-28 11:29:30.000000000 -0700
@@ -121,7 +121,7 @@
         uint256 _maxSortedUsers,
         address _cEth,
         address _wEth
-    ) external initializer {
+    ) public initializer { // HARNESS
         __ReentrancyGuard_init();
         __Ownable_init();
 
diff -ruN compound/PositionsManager.sol compound/PositionsManager.sol
--- compound/PositionsManager.sol	2022-06-21 09:26:03.000000000 -0700
+++ compound/PositionsManager.sol	2022-07-13 23:31:52.000000000 -0700
@@ -213,7 +213,7 @@
         address _onBehalf,
         uint256 _amount,
         uint256 _maxGasForMatching
-    ) external {
+    ) external override { //HARNESS : get a compiler error without this
         if (_onBehalf == address(0)) revert AddressIsZero();
         if (_amount == 0) revert AmountIsZero();
         _updateP2PIndexes(_poolToken);
@@ -302,7 +302,7 @@
         address _poolToken,
         uint256 _amount,
         uint256 _maxGasForMatching
-    ) external {
+    ) external override { // HARNESS
         if (_amount == 0) revert AmountIsZero();
         _updateP2PIndexes(_poolToken);
 
@@ -400,7 +400,7 @@
         address _supplier,
         address _receiver,
         uint256 _maxGasForMatching
-    ) external {
+    ) external override { // HARNESS
         if (_amount == 0) revert AmountIsZero();
         if (!userMembership[_poolToken][_supplier]) revert UserNotMemberOfMarket();
 
@@ -424,7 +424,7 @@
         address _onBehalf,
         uint256 _amount,
         uint256 _maxGasForMatching
-    ) external {
+    ) external override { // HARNESS
         if (_amount == 0) revert AmountIsZero();
         if (!userMembership[_poolToken][_onBehalf]) revert UserNotMemberOfMarket();
 
@@ -444,7 +444,7 @@
         address _poolTokenCollateral,
         address _borrower,
         uint256 _amount
-    ) external {
+    ) external virtual override {
         if (
             !userMembership[_poolTokenBorrowed][_borrower] ||
             !userMembership[_poolTokenCollateral][_borrower]
